for i=1,15,1 do
  turtle.select(16)
  gravel = turtle.compare()
  turtle.select(1)
  if gravel == false then
    turtle.dig()
    turtle.forward()
    for i=1,4,1 do
      turtle.digDown()
      turtle.down()
    end
    for i=1,3,1 do
      turtle.up()
    end
  end
end
